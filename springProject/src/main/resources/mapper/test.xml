<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">

	<select id="insert" resultType="testVO">
		insert into table1 values (#{idx}, #{day})
	</select>

	<select id="list" parameterType="testVO" resultType="testVO">
		select * from table1
	</select>


</mapper>
